import React from 'react'
const styles =(theme) => ({

})
const Billing = (props) => {
    const {classes} = prpos;
    return (
       <React.Fragment>

       </React.Fragment>
    )
}

export default withStyles(styles)(Billing);
